var donor = require('./donor.js');
var donation = require('./donation.js');
var recipient = require('./recipient.js');

module.exports = {
  donor: donor,
  donation: donation,
  recipient: recipient
}